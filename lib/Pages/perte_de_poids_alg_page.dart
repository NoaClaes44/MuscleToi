import 'package:flutter/material.dart';

class PerteDePoidsALGPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Programme Perte de Poids avec Allergies'),
        backgroundColor: Color(0xFF4B0DD8),
      ),
      body: Center(
        child: Text('Contenu de la page de perte de poids avec allergies'),
      ),
    );
  }
}
