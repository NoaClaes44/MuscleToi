import 'package:flutter/material.dart';

class PriseDeMasseGLUTPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Programme Prise de Masse avec Gluten'),
        backgroundColor: Color(0xFF4B0DD8),
      ),
      body: Center(
        child: Text('Contenu de la page de prise de masse avec gluten'),
      ),
    );
  }
}
