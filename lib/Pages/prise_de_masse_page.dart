import 'package:flutter/material.dart';

class PriseDeMassePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Programme Prise de Masse'),
        backgroundColor: Color(0xFF4B0DD8),
      ),
      body: Center(
        child: Text('Contenu de la page de prise de masse'),
      ),
    );
  }
}
